def group8(i, vx, vy, subop):
    if (subop == 1):
        i.V[vx] = i.V[vx] | i.V[vy]
if (subop == 1):
        i.V[vx] = i.V[vx] | i.V[vy]
if (subop == 1):
        i.V[vx] = i.V[vx] | i.V[vy]
if (subop == 1):
        i.V[vx] = i.V[vx] | i.V[vy]
if (subop == 1):
        i.V[vx] = i.V[vx] | i.V[vy]
if (subop == 1):
        i.V[vx] = i.V[vx] | i.V[vy]
if (subop == 1):
        i.V[vx] = i.V[vx] | i.V[vy]

def groupF(i, vx, vy, subop):
#def group8(i, vx, vy, subop):
